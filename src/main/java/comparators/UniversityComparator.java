package comparators;

import model.University;
import java.util.Comparator;

/**
 * Интерфейс-маркер для компараторов университетов, основанный на {@link Comparator}.
 */
public interface UniversityComparator extends Comparator<University> {
}
