package comparators;

import model.Student;
import java.util.Comparator;

/**
 * Интерфейс-маркер для компараторов студентов, расширяющий стандартный {@link Comparator}.
 */
public interface StudentComparator extends Comparator<Student> {
}
